.claude/
*.retry

# File di variabili "reali" con dati privati della tua infrastruttura
# (chiavi pubbliche, domini, IP, reti whitelistate...). Committa solo
# le versioni *.example: copiale senza suffisso e personalizzale in locale.
group_vars/all.yml
group_vars/mailservers.yml
group_vars/mx_backup.yml
host_vars/
inventory/hosts.ini

# Password generate da ispmail.sh, scaricate dal ruolo ispmail sul
# controller (contengono credenziali reali in chiaro)
secrets/
